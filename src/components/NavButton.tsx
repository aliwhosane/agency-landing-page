import React from "react";

const NavButton = () => {
  return (
    <button className=" bg-red-400 text-white px-6 py-2 rounded-full">
      Send a story
    </button>
  );
};

export default NavButton;
